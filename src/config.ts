/**
 * EW Signal App - Config (change these as needed)
 */

/** If primary threat confidence is above this (0–1), hide secondary/tertiary threats. */
export const HIGH_CONFIDENCE_THRESHOLD = 0.95;

/** Threat level scale 1 (danger) – 10 (safe) for gradient */
export const THREAT_LEVEL_MIN = 1;
export const THREAT_LEVEL_MAX = 10;
